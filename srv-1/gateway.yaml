apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: istio-srv1-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - srv-1.istio-demo.intra.net